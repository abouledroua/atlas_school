import 'package:atlas_school/view/widget/mywidget.dart';
import 'package:flutter/material.dart';

class DeconnecterWidget extends StatelessWidget {
  const DeconnecterWidget({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return MyWidget(child: Container());
  }
}
