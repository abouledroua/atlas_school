import 'package:flutter/material.dart';

class ListPhotos extends StatelessWidget {
  const ListPhotos({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Text("ListPhotos");
  }
}
